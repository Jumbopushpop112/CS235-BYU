# BYU Grep

Write a command-line utility like `grep` that prints out all lines of the input that have the provided substring.

```
./byugrep vitamin content.txt
```

Write your own method to determine whether a given string is a substring of another.

If no filename is provided, read from stdin.

If time permits, support case insensitivity.

